# 前端环境变量示例（复制为 .env，.env 不进 Git）

# 后端 BFF 地址
VITE_API_BASE=http://localhost:8000

# 高德 JS API 2.0（Web端 JS API）——控制台 https://console.amap.com/dev/key/app
# 应用管理→我的应用→创建应用→添加Key→服务平台选「Web端(JS API)」
VITE_AMAP_JS_KEY=
# 安全密钥(jscode)：JS API 2.0 必填，与 Key 同时获取，否则地图不出图
VITE_AMAP_SECURITY_CODE=
